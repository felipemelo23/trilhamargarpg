<div class="trilhamarga chat-card npc-ability">
    <header class="card-header flexrow">
        <img src="{{actor.img}}" title="{{actor.name}}" width="36" height="36"/>
        <h3 class="actor-name">{{actor.name}}</h3>
        {{#if item.img}}<img src="{{item.img}}" title="{{item.name}}" width="36" height="36" class="item-icon"/>{{/if}}
    </header>

    <div class="card-content">
        <h4 class="ability-name">{{abilityName}}</h4>
        {{#if description}}
        <div class="ability-description">{{{description}}}</div>
        {{/if}}
    </div>
</div>